# Jaguar Frontend V1.8 — Mobile / APK privado

Escopo implementado:
- aba `/aplicativo`;
- instruções de fontes desconhecidas / instalação direta;
- status automático do APK via HEAD `/api/mobile/apk`;
- QR Code para download quando APK estiver publicado;
- proxy Bearer para aplicativo Android;
- upload de foto real de produto com sessão validada;
- armazenamento em volume persistente configurável;
- imagem vinculada ao campo `products.image_url` já existente;
- nenhuma alteração necessária no schema PostgreSQL ou nos workflows n8n existentes.
